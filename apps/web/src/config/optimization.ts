// Export empty object since we're not using dynamic imports for now
export const dynamicImports = {};

// Configuración de preload
export const preloadComponents = () => {
  // No-op since we're not using dynamic imports
};
